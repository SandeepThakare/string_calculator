/**
 * Utils module entry point
 * Exports DELIMITERS and MESSAGES constants.
 * @module utils
 */
const { DELIMITERS, MESSAGES } = require('./constants');

module.exports = {
  DELIMITERS,
  MESSAGES
};