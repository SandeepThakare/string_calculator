/**
 * Calculator module entry point
 * Exports the StringCalculator class for use in other modules.
 * @module calculator
 */
const StringCalculator = require('./stringCalculator');

module.exports = {
  StringCalculator
};